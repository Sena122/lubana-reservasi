{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "node .railway/build.sh || php artisan serve --host=0.0.0.0 --port=${PORT}",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100
  }
}